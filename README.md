Designed and implemented a distributed system for fuzzy text searching within large datasets using the Damerau-Levenshtein algorithm. Built on a Master-Worker architecture, the system utilizes data parallelism by splitting files into chunks and applying a mathematically calculated overlap strategy to guarantee accurate pattern detection across partition boundaries. For more read the documentation.
